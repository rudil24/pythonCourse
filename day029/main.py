# rudil24
# Day 29: Password Manager Project using TK Canvas for the GUI

from password_manager import PasswordManager

# This block ensures the code runs only when executed directly,
# not when imported as a module in another script. It's the standard entry point pattern.
if __name__ == "__main__":
    # Instantiate the class and start the application loop.
    app = PasswordManager()
    app.run()
